# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://help.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/" # Location of package manifests
    schedule:
      interval: "daily"
    # apernet/quic-go is a deliberate fork. Newer tags often ship a go.mod that
    # declares module github.com/quic-go/quic-go, which makes Dependabot fail
    # with go_module_path_mismatch and cannot be auto-bumped safely.
    ignore:
      - dependency-name: "github.com/apernet/quic-go"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
